---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: slurmbridgejobs.kubecluster.org.kubecluster.org
spec:
  group: kubecluster.org.kubecluster.org
  names:
    kind: SlurmBridgeJob
    listKind: SlurmBridgeJobList
    plural: slurmbridgejobs
    shortNames:
    - slurmbridgejob
    - sbj
    singular: slurmbridgejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.state
      name: State
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SlurmBridgeJob is the Schema for the slurmbridgejobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SlurmBridgeJobSpec defines the desired state of SlurmBridgeJob
            properties:
              partition:
                type: string
              sbatch_script:
                type: string
            required:
            - partition
            - sbatch_script
            type: object
          status:
            description: SlurmBridgeJobStatus defines the observed state of SlurmBridgeJob
            properties:
              array_job_id:
                type: string
              batch_host:
                type: string
              exit_code:
                type: string
              id:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              name:
                type: string
              node_list:
                type: string
              num_nodes:
                type: string
              partition:
                type: string
              run_time:
                description: A Duration represents the elapsed time between two instants
                  as an int64 nanosecond count. The representation limits the largest
                  representable duration to approximately 290 years.
                format: int64
                type: integer
              start_time:
                format: date-time
                type: string
              state:
                type: string
              std_err:
                type: string
              std_out:
                type: string
              submit_time:
                format: date-time
                type: string
              time_limit:
                description: A Duration represents the elapsed time between two instants
                  as an int64 nanosecond count. The representation limits the largest
                  representable duration to approximately 290 years.
                format: int64
                type: integer
              user_id:
                type: string
              work_dir:
                type: string
            required:
            - array_job_id
            - batch_host
            - exit_code
            - id
            - name
            - node_list
            - num_nodes
            - partition
            - run_time
            - start_time
            - state
            - std_err
            - std_out
            - submit_time
            - time_limit
            - user_id
            - work_dir
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
